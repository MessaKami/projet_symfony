{% extends 'base.html.twig' %}

{% block title %}Edit User
{% endblock %}

{% block body %}
	<section class="container">
		<h1>Edit User</h1>
		<div class="row">
			<div class="col">
				
				{{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

				<a href="{{ path('app_user_index') }}" class="btn btn-secondary mt-1">Retour au pannel</a>

				{{ include('user/_delete_form.html.twig') }}
			</div>
		</div>
	</section>

{% endblock %}
