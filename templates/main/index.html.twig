{% extends 'base.html.twig' %}

{# {% block title %}Accueil!{% endblock %} #}

{% block body %}
	<main class="container">
		<section class="row">
			<div class="col-12">
				<h1>Site e-commerce</h1>
				<p>Accueil</p>
			</div>
			{% for user in user %}
				<div class="col-12">
				{{ user.firstname}}
				</div>
			{% endfor %}
			<div class="col-12">
				{{ dump() }}
			</div>
		</section>
	</main>

{% endblock %}
